node_modules
dist
.DS_Store
*.local
.env
.env.local

# Local Netlify folder
.netlify

# TypeScript incremental build cache
tsconfig.tsbuildinfo

# Claude Code per-machine settings
.claude/settings.local.json

# Deno lockfile (project does not use Deno)
deno.lock

# Personal notes / non-site assets
Portifolio pessoal IA.txt
screenshots-linkedin/

# Local blog post drafts (uploaded to Drive, not committed here)
/*.md
!/README.md

# Local reference HTML used to build /aerolito-intro.html (not committed)
scripts/_aerolito-reference.html
